ECHO is on.

Testing Plan for Project Luka:

1. **load_data function**:

2. **process_events Function**:

3. **display_inventory Function**:


Unit Test Driver Program:

#include <iostream>
#include <map>
#include <array>
#include <list>
#include <string>
#include "Dog.hpp"

using namespace std;

// Function prototypes
void load_data(const string& fileName, map<string, array<list<Dog>, 3>>& inventory);
void process_events(map<string, array<list<Dog>, 3>>& inventory, int period);
void display_inventory(const map<string, array<list<Dog>, 3>>& inventory, int period);

int main() {
    map<string, array<list<Dog>, 3>> dogInventory;

    // Test load_data function
    load_data("dogs_data.txt", dogInventory);
    cout << "Data loaded successfully.\n";

    // Test display_inventory function
    display_inventory(dogInventory, 0);

    // Test process_events function
    process_events(dogInventory, 1);
    display_inventory(dogInventory, 1);

    return 0;
}